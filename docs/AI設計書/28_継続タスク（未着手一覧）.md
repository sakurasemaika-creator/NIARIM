# 28_継続タスク（未着手一覧）

このファイルは、ユーザーから2026年8月19日に連続して依頼された大量の要望・不具合報告のうち、
まだ着手できていない項目を別セッションへ引き継ぐためのメモ。上から順に着手して問題ない
（依存関係のあるものは注記した）。着手前に必ず現状のコードを確認すること（このメモの記述後に
状況が変わっている可能性がある）。

作業時の基本フロー（このセッションで確立した手順、必ず踏襲する）：
`flutter analyze`（ベースライン67件、0エラー、既存のdeprecated_member_use/use_build_context_synchronouslyのみ）
→ `flutter test`（ベースライン72件全成功）→ `git add -A` → 日本語コミットメッセージ
（末尾に`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`）→
`git fetch origin dev_branch && git log HEAD..origin/dev_branch --oneline`（分岐確認）→
`git push origin dev_branch`。ARBファイルを編集したら必ず`flutter gen-l10n`を実行し直す
（7言語：ja/en/es/fr/ko/zh/zh_Hant、jaがテンプレート）。

## 既にこのセッションで完了済み（再着手不要・前提として押さえておくこと）

- エンドカード：差し替え不可・無料会員はタップでプレミアム誘導のみ・プレミアム会員は削除
  （非表示）のみに修正済み。設定＞ワークスペース設定に「エンドカードを最初から非表示にする」
  プレミアム限定トグルを追加済み（プレミアム失効で自動リセット）。
- 「囲って塗る」機能：**既に実装済み**だった（投げ縄塗り選択中、キャンバス上部バーに
  「囲って塗るモードスイッチ」が表示される）。新規実装は不要。
- フレーム一覧・ツールバーの折りたたみ機能：**既に実装済み**だった
  （`_showFrameStrip`/`_showToolbar`、開閉ボタン付き、デフォルト開いた状態）。
- 投げ縄塗り：ペンのサブツールからバケツ長押しメニューへ移設済み
  （`toolbar_widget.dart`の`_showBucketToneMenu`、`LassoFillToneSheet`）。
- 選択ツール・消しゴム・図形・バケツ塗りのアイコンをFont Awesome Free
  （`font_awesome_flutter`パッケージ）へ差し替え済み。ライセンス画面
  （`license_screen.dart`）に「使用アイコンについて」クレジット追加済み。
- ツールオプション系フローティングパネル（ブラシ・トーン・スタンプ・ペンサブツール・
  オニオンスキン・定規・フィルター・早替え設定・レイヤー）を画面全体の最前面Stackへ移動済み
  （`canvas_screen.dart`のbuild()末尾）。パネル外タップで閉じるバリアも追加済み。
  ペンの長押しに加え上スワイプでも開けるようにした。
- レイヤーパネル：ゴミ箱アイコンとドラッグハンドルの重なりを修正済み
  （`buildDefaultDragHandles: false`＋明示的な`ReorderableDragStartListener`）。
  複数選択モードでの一括削除・一括結合を廃止し、各行に「下のレイヤーと結合」ワンタップ
  ボタン、ヘッダーに「表示中の全レイヤーを結合」ワンタップボタンを追加済み
  （複数選択モード自体はグループ化専用として存続）。
- ブラシの太さ・不透明度スライダー：デフォルト折りたたみ＋タップで展開、展開時も
  スライダー間隔を詰めるよう修正済み（`brush_size_slider.dart`）。
- ツールバーの高さを48px→40pxへ縮小済み。
- 保存失敗時、無理に画面遷移せず「保存に失敗しました」ダイアログを出して留まるよう修正済み
  （`timeline_screen.dart`の`_saveAndGoToCanvas`/`_confirmBackToProjectList`）。
  **ただし保存がなぜ失敗するのか根本原因は特定できていない**（try/catchで症状を防いだのみ）。
- フレーム一覧サムネイルのセル背景を、実際のキャンバス背景と異なる濃いグレーから白へ修正済み。
- プロジェクト詳細画面の「セーブツリー」ボタンが、パフォーマンス設定のセーブ方式次第では
  実際には「セーブスロット」画面を開いてしまいボタン名と食い違っていた不具合を修正済み
  （現在の方式に応じてボタン文言・アイコンが動的に切り替わるようにした）。
- タイムラインの新機能「タイムスタンプ」（マーカー機能）実装済み。演出フィルター・共通レイヤー・
  動画・音声トラックを「使用中のみ表示」方式に変更済み。プレビュー欄の高さドラッグハンドル実装済み
  （ただしユーザーから「表示範囲が変わってサイズが連動しない」との報告が一度あり、コード上は
  正しく高さ連動しているように見えるが根本原因未特定。ヒット領域は拡大済み）。
- Tips画面をポップアップ形式に刷新済み（可変ページ数、右上閉じるボタン、下部ページ
  インジケーター）。当たり前すぎるTipsの削除、透明色・早替えツールのブラシ/サイズ違い・
  共通レイヤー×口パク瞬き容量削減・共通レイヤー×レイヤーキーフレーム容量削減・引き継ぎ機能・
  ブレンドモード使い分けのTipsを追加済み。
- ヘルプのブレンドモード説明を17種類全て個別解説する詳細版に変更済み。
- ヘルプ「プレミアム」の説明文修正済み（エンドロゴ「削除」に訂正、尺制限を具体的に「90秒」、
  広告非表示を明記。期間限定キャンペーンの記述は修正範囲拡大を避けるため意図的に据え置き）。
- ヘルプの以下の記述を削除済み：動画書き出しのH.264ライセンス懸念、GIF書き出しの
  「専用GIFモードはない」という否定的説明、カラーピッカーの「長押しで説明表示」。
  **ただし他のヘルプ項目にも同様の記述が残っている可能性が高く、全文チェックはしていない。**

## 未着手：ヘルプページ全体の再編（大規模）

- ~~図形ツールのヘルプ項目に、どんな図形があるか等の項目内説明を全て記載する。~~
  → **確認済み・対応不要**：既に「直線・四角形・円」（`ShapeKind`enumの全種類）を
  明記済みだった。
- ~~フォント管理の説明をテキストツールのヘルプ項目に統合する。~~ → **対応済み**。
- ~~スタンプの回転・拡大縮小の説明をスタンプツールのヘルプ項目に統合する。~~ → **対応済み**。
- ~~共通レイヤーのヘルプ項目に「レイヤーの共通化」の説明を統合する。~~ → **対応済み**。
- ~~自動塗りのヘルプ項目に、自動塗りプリセット・自動塗り実行・線画色設定の説明を統合する。~~
  → **対応済み**。
- 統合しきれない項目も、関連する項目同士がヘルプ一覧内で近くに並ぶよう順番を入れ替える。
  → 今回のバッチでは本格的な並び替えは未実施（既存の大分類ごとのセクション分けで
  ある程度は達成されている）。
- ~~描画フィルターと演出フィルターの違いをヘルプページに明記する（画面ごとに項目が並ぶように）。~~
  → **確認済み・対応不要**：`helpDrawingFilterDesc`側に既に
  「演出フィルターがタイムライン全体・シーン単位に適用されるのに対し、描画フィルターは
  レイヤー単位」という区別が明記されていた。
- ~~個々の描画フィルター・演出フィルターの説明を独立した1項目にせず、
  「描画フィルター」「演出フィルター」という親項目の中にまとめる~~ → **対応済み**：
  演出フィルターの適用順・動くノイズフィルター・雨フィルターを「演出フィルター」へ統合した
  （描画フィルター側は元々個別項目化されていなかった）。
- ~~書き出し関連のヘルプ項目をある程度1つにまとめる。~~ → **対応済み**：透過WebM・
  GIF書き出し・無料会員の尺制限を「動画書き出し」（タイトルを「動画書き出し
  （MP4・WebM・GIF）」へ改称）へ統合した。「書き出し画面」は画面自体の説明として別項目に
  残した。`helpTransparentWebmTitle`は`export_screen.dart`のWebM形式選択ラジオボタンの
  ラベルとして実際に使われていたため、Title側のみ削除せず維持している。
- ヘルプページをコードで実際の画面を再現した図解付きにする（Tips画面の`TipDiagramKind`と
  同様のCustomPainterベースの図解を想定。当該ボタンの箇所に白縁取りの赤丸や矢印を付けると
  分かりやすい、との指定あり）。→ **未着手**：対象項目数が多く（60件超）、独立した大きめの
  バッチとして別途対応が必要。Tips画面のように少数の汎用図解パターン（ツールアイコン＋
  タップ位置、長押しジェスチャー、2本指ジェスチャー、スライダー操作、パネルレイアウト等）を
  用意し、各ヘルプ項目へ割り当てる方式が現実的と思われる。
- ~~ヘルプ全文から、ライセンス上の懸念・開発の都合・存在しない機能の説明などユーザーに
  無関係な記述がないか総点検する~~ → **対応済み**：全help系ARBキーを対象に
  「ライセンス」「特許」「開発中」「未実装」「対応予定」「今後」「将来的」等の
  キーワードで機械的に検査し、該当なしを確認した。
- ~~選択範囲の変形の説明に、自由変形とメッシュ変形の説明を追加する（下記の新機能と対になる）。~~
  → **対応済み**：新規ヘルプ項目「自由変形・メッシュ変形」を追加し、「選択範囲の変形」の
  説明文からも言及するようにした。
- ~~早替えツールは異なるツールだけでなくブラシ違いのペンやサイズ違いの消しゴムも登録できる、
  という説明をヘルプかTipsに追加する~~ → **確認済み・対応不要**：Tips画面の
  `tipsQuickToolVariantTitle`/`Desc`で既に説明済みであることを確認した（重複を避けるため
  ヘルプ側への追記は見送り）。

## 未着手：新機能実装

- ~~**レイヤー全体の自由変形・メッシュ変形ツール**~~ → **対応済み**：
  キャンバスモード右上の編集・設定ボタン内に「自由変形・メッシュ変形」項目を追加した。
  範囲選択せずに現在使用中のレイヤー全体が対象（既存の`DrawingTool.transform`は4隅一体の
  移動・拡大縮小・回転のみだったのに対し、こちらは格子点を個別にドラッグできる）。
  `engine/mesh_warp_engine.dart`（`ui.Vertices`＋`ImageShader`によるGPUワープ、単体テスト
  5件で恒等変形・拡大変形をピクセル単位検証済み）と`TileManager.meshTransformLayer()`で
  実際の変形を行い、`canvas_area.dart`側で1本指＝1格子点のドラッグ（複数指で同時に別々の
  角をつまめば自然に回転・拡大縮小相当になる）とライブプレビュー描画を実装した。
  分割数（1〜10×10）・回転・拡大縮小はコントロールパネルのスライダーから、確定・
  キャンセルは同パネルのボタンから操作する。詳細は`12_実装チェックリスト.md`の
  「レイヤー全体の自由変形・メッシュ変形ツールを実装」を参照。
- ~~**長押しスポイトのON/OFF設定**~~ → **対応済み**：
  設定画面「ジェスチャー」に「長押しスポイト」セクションを追加し、ON/OFF（既定ON）と
  保持秒数（0.2〜3.0秒、既定0.5秒）を設定できるようにした。
  `canvas_area.dart`：ペン（トーン/スタンプサブツールを除く通常描画）・消しゴムでの
  ストローク開始時に保留タイマーを仕込み、指定px以上動く・複数指になる・指を離すまでに
  保持時間へ到達しなければ通常のストロークとしてそのまま継続する。保持が成立した場合は、
  `TileManager.beginUndoRecording`/`endUndoRecording`が既に記録している「変更前」
  タイルスナップショットを`applyTileSnapshot`で直接書き戻すことで、Undo履歴を汚さずに
  それまで描かれていた分を巻き戻し、その位置の色をスポイトのように拾って現在色へ反映する
  （このポインターは以後の描画ツール処理から除外し、離すまでの残りのmove/upは無視する）。
  対象をペン・消しゴムに限定した理由：バケツ・投げ縄・選択・図形・変形等は元々タイルへ
  即時書き込みしない（ローカル状態のみで完結する）か、バケツのように長押し自体が
  既に別の意味（連続塗り）を持つため。`test/tile_manager_test.dart`に、この巻き戻し
  機構（`beginUndoRecording`→書き込み→`endUndoRecording`のbeforeスナップショットを
  `applyTileSnapshot`で復元）が正しく動作することを検証するテストを追加した。
- ~~**マーカーペン・カリグラフィーブラシをデフォルトで実装**~~ → **対応済み**：
  `models/brush.dart`に`calligraphyAngle`（ペン先の固定角度、度）を追加し、
  `drawing_engine.dart`の`_stampBrush()`で、指定時は実際のスタイラス傾きに
  関わらず常にその角度へ扁平化したペン先（既存の傾き検知用スタンプ変形の仕組みを
  流用、扁平度`kCalligraphyNibAspect=2.6`）でスタンプするようにした。
  `brush_service.dart`の初期ブラシへ「マーカーペン」（`strokeDecay: true`で
  実物のインク切れを表現、筆圧反映OFF）・「カリグラフィー」
  （`calligraphyAngle: 45.0`）を追加し、既存ユーザーにも起動時に自動追加する
  （ToneServiceの初期データ追加と同じバックフィル方式）。`niatra_serializer.dart`の
  ブラシ引き継ぎフォーマットにも`calligraphyAngle`を追加済み。
- ~~**筆圧カーブへのアクセス経路追加**~~ → **対応済み**：
  `canvas_screen.dart`の「設定/編集」メニュー（`_showEditMenu`）へ
  「筆圧カーブ」項目を追加し、`/settings/pen`（設定画面と共通の同じ画面）を
  直接開けるようにした。設定値自体が1箇所（SettingsService）に集約されて
  いるため、どちらから開いても同じ値を編集することになる。
- ~~**自動塗りの線画色設定にデフォルトへ戻すボタン**を実装する。~~ → **対応済み**：
  `autofill_preset_screen.dart`の線画色（specified）指定パネルへ、黒（デフォルト値
  `0xFF000000`）へ戻す`TextButton.icon`（`Icons.restart_alt`）を追加。既にデフォルト値の
  場合は非活性にする。
- ~~**書き出し時・複数フレーム加工時の進捗表示にTips表示を追加**~~ → **対応済み**：
  `widgets/progress_dialog.dart`に、10秒おきにランダム表示されるTipsを追加した。
  正方形広告とTipsの両方を同時に出すとダイアログが煩雑になるため、指定された
  代替案の通り「プレミアム会員（広告なし）はTips、無料会員（広告あり）は従来通り
  広告のみ」という排他表示にした。Tipsの内容は`tips_screen.dart`（Tips画面）の
  一覧をそのまま流用する新規公開関数`allTipEntries()`から取得するため、
  Tips画面と内容が二重管理にならない。

## セーブ／自動保存の再設計（大きめ） → 対応済み

- ~~プロジェクトを開いたとき、自動保存データがあれば最後の自動保存データから自動的に
  復元して再開する。~~ → **対応済み**：`canvas_screen.dart`の`_checkCrashRecovery`から
  確認ダイアログを撤去し、無条件で自動復元するよう変更した。
- ~~手動保存データからの復元は、以下の2つの入口からのみ行えるようにする~~ → **対応済み**：
  `SaveTreeEntryMode`（timeline/projectDetail/quickSave）をルーティングの
  クエリパラメータ経由で伝え、キャンバス画面の保存ボタン（quickSave）からは
  復元操作自体を無効化（アイコン・メニュー項目を非表示に）した。
  1. タイムラインモードから手動保存画面（セーブツリー／セーブスロット）を開いたとき
  2. プロジェクト詳細画面からセーブデータ一覧を開いたとき
- ~~**タイムラインモードから**セーブデータ一覧を開いて手動保存データをタップした場合：
  「上書きする」か「ここから再開する」かを尋ねるポップアップを出す。~~ → **対応済み**：
  「上書きする」は既存の上書き保存ダイアログ（スロット方式）／このノードを親とした
  新規保存ダイアログ（ツリー方式）を再利用しつつ、「このときのセーブデータは消えますが
  よろしいですか」の確認を追加。「ここから再開する」は「セーブしていない場合は現在の
  データが消えますがよろしいですか」の確認を追加した（実装は`_handleSaveNodeRestore()`
  へ集約）。
- ~~**プロジェクト詳細画面から**セーブデータ一覧を開いた場合：
  セーブデータをタップしたら「ここから作業を再開する」か「キャンセルして閉じる」かを
  選択するだけのシンプルなポップアップにする（上書きの概念はここにはない）。~~
  → **対応済み**。

## 未着手：個別の不具合・細部の要望

- **フレーム一覧をタップしても現在地（カレントフレーム）が移動しない**（キャンバスモード側の
  `FrameStripWidget`）。タップしたフレームへ現在地を移動するよう修正する。
  タイムライン側は既に動いている可能性があるので比較すること。
  → **コードレビュー実施済み・バグ見つからず**：各フレームの`GestureDetector.onTap`は
  `widget.onFrameSelected(index)`を呼び、`canvas_screen.dart`側で
  `onFrameSelected: (idx) => setState(() => _currentFrame = idx)`と正しく配線されている。
  `IgnorePointer`で囲われた中央の赤枠がタップを奪っている可能性も確認したが、
  `IgnorePointer`はヒットテスト対象から正しく除外されるため問題なし。実機がなく
  最終確認はできていないが、静的解析上は現状のコードで動作するはず。再現するようなら
  実機のログ（onTapが実際に呼ばれているか）から調査すること。
- **フレーム一覧を左右にスワイプしたとき、中央の赤枠に一番近いフレームへ自動吸着し、
  そのフレームを現在地にする**（タイムラインモードと同じ挙動に揃える）。
  そのときキャンバス・レイヤーパレット・タイムラインモードの表示も連動すること。
  → **コードレビュー実施済み・バグ見つからず**：`_scrollToCurrent`の
  `target = currentFrame * itemExtent`と`_handleScrollEnd`の
  `nearest = (offset / itemExtent).round()`は、`sidePadding = (ビューポート幅-itemExtent)/2`
  を用いた中央揃えの計算式として数式上つじつまが合っている（左右パディング分が
  ちょうど打ち消し合う）ことを手計算で確認した。実機での動作確認ができておらず、
  再現するなら実機で確認すること。
- ~~**フレーム一覧を最小化していても、キャンバス外の右側をダブルタップで次のフレームへ、
  左側をダブルタップで前のフレームへ移動できるようにする**~~ → **対応済み**：
  `canvas_area.dart`の外側`Listener`最上流で、画面左右端の狭い帯（32px）を
  「キャンバス外」のジェスチャー専用ゾーンとして扱い、既存の描画パイプラインには
  一切手を入れずに、350ms以内の同じ側への2回タップを検出して
  `onNextFrame`/`onPreviousFrame`（`canvas_screen.dart`側、フレーム範囲でクランプ）を
  呼ぶだけの実装にした。フレーム一覧（`FrameStripWidget`）の開閉状態と無関係に常時有効。
- ~~**プロジェクト詳細画面のプレビューに、シークバー・全画面化・縮小表示機能を追加する**~~
  → **対応済み**：`project_detail_screen.dart`に、タイムラインモードのプレビューと同じ
  パターンで①シークバー（`_buildSeekBar`、ドラッグで任意フレームへ直接移動）、
  ②全画面化ボタン（`_isPreviewFullscreen`、タップで黒背景の全画面Scaffoldへ切替、
  同じシークバー・再生コントロールを再利用）、③縮小表示トグル（`_isPreviewCollapsed`、
  通常のAspectRatio(16:9)と高さ72pxの帯状表示を切り替え）を追加した。
- **定規ツールをタップしても何も表示されない**という報告があった。
  → **コードレビュー実施済み・バグ見つからず**：`_showRulerPanel`は他の
  正常動作しているパネル（ブラシ・トーン・スタンプ等）と全く同じ
  `_sidedPanel(...)`パターンで画面全体の最前面Stackに配置されており、
  `_toggleRuler`（トグル）・`_rulerPanel()`（内容）とも配線に問題は見当たらない。
  フローティングパネルを最前面Stackへ移した修正で解消している可能性が高いが、
  実機での最終確認はできていない。
- **プレビュー下端のドラッグハンドルを動かすと「表示範囲」が変わってしまい「サイズ」が
  連動しない**という報告が一度あった。コードレビューでは`RawImage(fit: BoxFit.contain)`が
  使われており、外側のSizedBoxの高さを変えるだけの実装で理論上は正しくサイズ連動する
  はずだが、根本原因は特定できていない（ヒット領域拡大のみ対応済み）。実機で再現するか
  要確認。
- ~~**ツール詳細設定ポップアップの操作方法統一**~~ → **対応済み**：
  ①上スワイプで開く：バケツ（`_showBucketToneMenu`）・選択ツール
  （`_showSelectMenu`）に、ペン・早替えツールと同じ`onVerticalDragEnd`
  （上向きの速い動きでlongPressと同じ処理を呼ぶ）を追加した。
  ②中央×で閉じる：ブラシ・カラーピッカー・フィルター・レイヤー・
  オニオンスキン・早替えツール・定規・スタンプ・トーンの9パネル全てで、
  タイトル行右端にあった×ボタンを削除し、共通ウィジェット
  `PanelCenterCloseBar`（`panel_close_bar.dart`、新規）をパネル最上部に
  配置する形へ統一した（画面中央ではなく各パネル自身の横幅の中央。
  パネルは左右どちらかの画面端にドッキング表示されるアーキテクチャ自体は
  維持しつつ、閉じるボタンの位置だけを指定通り「パネル中央」へ変更した）。
- ~~**長押しでチュートリアルが表示される件**~~ → **対応済み**：
  `widgets/first_use_tooltip.dart`の発火トリガーを`onTapDown`（押した瞬間に
  無条件で発火し、後から長押し/ドラッグに負けてもキャンセルされない）から
  `onTap`（同じジェスチャーアリーナ内の長押し認識に負けた場合は呼ばれない）へ
  変更した。これにより「長押しで機能を開くボタンは、長押し操作時にチュートリアル
  自体を表示しない」という要件を満たす。IconButtonの`tooltip:`パラメータ経由の
  標準Tooltipとの衝突可能性については、今回は`FirstUseTooltip`側の修正のみに
  留めており、個別の組み合わせの全数調査はまだ行っていない。
- **チュートリアルの吹き出しがどこをタップしても消えない**という報告があったが、
  `first_use_tooltip.dart`を確認した限り、既に全画面の透明バリアで「どこでも閉じられる」
  実装になっていた。多重表示バグ（このセッションで修正済み）が原因だった可能性がある。
  実機で再現するか要確認。
- **ヘルプ全文からライセンス懸念・開発都合・存在しない機能の説明を除去**（前述、再掲）。

## 未着手だが優先度が低いと思われるもの（ユーザーに確認してから着手推奨）

- カメラキーフレーム・演出フィルターへの「列追加」対応の要否再検討
  （前回、カメラは1フレーム1値のため列追加不要と判断して単一トラックのままにした旨を
  報告済み。ユーザーから追加の反応待ち）。

## 進め方の注意

- このメモに挙げた項目は非常に多い。一度に大量に手をつけると検証が追いつかず品質が
  落ちるため、**必ず数件ずつ着手→analyze/test→コミット→pushのサイクルを回すこと**。
- 実機で確認しないと真偽が分からない項目（フレーム一覧タップ・ドラッグハンドルの挙動・
  定規ツール等）は、コードレビューだけで「直った」と断定せず、正直に「直っている可能性が
  高いが要確認」と報告すること。
