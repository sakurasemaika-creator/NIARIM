# 28_継続タスク（未着手一覧）

このファイルは、まだ解決できていない項目のみを別セッションへ引き継ぐためのメモ。
完了した項目は都度削除しているため、記載があるものはすべて未着手か要確認の状態。
着手前に必ず現状のコードを確認すること（このメモの記述後に状況が変わっている
可能性がある）。

作業時の基本フロー（必ず踏襲する）：
`flutter analyze`（ベースライン73件、0エラー、既存のdeprecated_member_use/use_build_context_synchronouslyのみ）
→ `flutter test`（ベースライン104件全成功）→ `git add -A` → `git restore --staged pubspec.lock`
（ローカルの`flutter run`で更新されるため必ずコミットから除外する）→ 日本語コミットメッセージ
（末尾に`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`）→
`git fetch origin dev_branch && git log HEAD..origin/dev_branch --oneline`（分岐確認）→
`git push origin dev_branch`。ARBファイルを編集したら必ず`flutter gen-l10n`を実行し直す
（7言語：ja/en/es/fr/ko/zh/zh_Hant、jaがテンプレート）。ARB翻訳は機械的な文字列置換だと
ES/FRなどで訳が崩れることがあるため、ロケールごとに手書きで訳すこと。ソースコードの
コメントには仕様書番号・タスク番号・経緯の記述を含めず、コードの技術的な説明のみを書くこと。

## 完了済み（このセッションで対応・コミット・プッシュ済み、参考記録）

- 自律スモークテストを「画面が例外なく描画できるか」から「その画面に見えて
  いる操作可能な要素（ボタン・アイコンボタン・リストタイル・チェックボック
  ス・スイッチ・チップ・カード等）を実際に自動で1つずつ操作しても例外が
  起きないか」まで検証できるよう拡張した。`test/app_smoke_test.dart`に
  `probeAllControls`（画面上の操作可能要素をツリーから走査し、
  `ModalRoute.isCurrent`で「今アクティブな画面に属する要素か」を判定して
  未タップのものを1つずつタップ→ダイアログ/ボトムシート/新しい画面が
  開けば`ModalRoute`の同一性を基準に自動でNavigator.pop()して戻る、を
  新しい要素が見つからなくなるまで繰り返す汎用ヘルパー）を新設し、
  `visitRoutesAndPop`（既存の11設定画面等の巡回に使用）へ組み込んだ。

  実装時の留意点：GoRouterで`push`した前画面がツリーに残ったまま
  （`ModalRoute.isCurrent`がfalse）になるため、これを見ずに全ツリーを
  そのまま操作対象にすると、隠れた前画面の要素を誤ってタップしてしまう
  （実際に発生し、修正した）。ダイアログの「戻る」判定もScaffold数の
  増減という間接的な指標では、閉じるアニメーションが完全に収まる前に
  判定すると余分にpop()してしまい画面ごと戻りすぎることがあったため、
  `ModalRoute`のインスタンス同一性で直接判定する方式に変更した。
  標準ボタン（ElevatedButton等）は内部実装にInkWellを持つため、候補型の
  祖先を持つ要素は除外し、独自のInkWell/GestureDetector（カード等）と
  二重に検出しないようにした。file_picker等、`flutter test`環境に登録
  されていないプラグインは、呼び出すと`tester.takeException()`を経由
  しない捕捉不能な形で例外が飛ぶため、`FilePicker.platform`をnullを返す
  フェイク実装へ差し替えて回避した。

  **この仕組み自体が、本セッション中に実際の不具合を発見するきっかけに
  なった**：`showDialog(...).then((_) => controller.dispose())`という
  パターン（ダイアログの`Future`解決直後にTextEditingControllerを破棄する）
  が、ダイアログの閉じるトランジションアニメーション中にまだTextFieldが
  そのcontrollerを参照している状態と競合し、「TextEditingController was
  used after being disposed」で頻発にクラッシュしうる不具合が、
  `EditableSliderValue`（バケツ塗り設定等の数値入力ダイアログ）・
  プロジェクト詳細画面の名前変更/タグ追加ダイアログ・セーブツリーの
  保存コメントダイアログ・タイムラインの各種名前変更/マーカー編集
  ダイアログ・ワークスペース/フォント/ウォーターマーク設定の各種名前
  変更ダイアログ・キャンバスのテキスト入力ダイアログ・レイヤーパネルの
  グループ作成/レイヤー名変更ダイアログ・カラーピッカーのパレット作成/
  名前変更ダイアログ・自作ブラシ/トーン/スタンプのフォルダ名/アセット名
  入力ダイアログ、計19箇所で見つかった。`lib/widgets/dispose_on_unmount.dart`
  に、controllerの破棄をState.dispose()（ウィジェットが実際にツリーから
  外れるタイミング）へ委ねる汎用ラッパー`DisposeOnUnmount`を新設し、
  全箇所へ適用して修正した（`EditableSliderValue`のみ、ダイアログ内容が
  シンプルだったため専用のStatefulWidgetへ直接書き換える形で対応）。

- 起動画面のボタン文言変更：「みんなのアニメを見る」→「みんなの／作品をみる」
  （改行あり）、「アニメを作る」→「作品をつくる」（ひらがな表記）。
- 無料版エンドカードの中央にアプリロゴ（SVG）を描画するよう変更
  （`export_engine.dart`の`_renderEndCardPng`）。実機での見た目は未検証。
- ショートカット設定にコピー・切り取り・貼り付け・全選択（Ctrl+C/X/V/A）を
  追加。ホーム画面（プロジェクト/フォルダ）・キャンバスモード（レイヤー、
  ただし切り取りは未対応）・タイムラインモード（Ctrl+Aのフレーム/シーン
  全選択のみ）に対応済み。タイムラインのコピー/切り取り/貼り付けは上記の
  「未着手」セクションを参照。
- 自律スモークテスト（`test/app_smoke_test.dart`）を14シナリオへ拡張
  （起動→ホーム→設定→ショートカット設定／新規プロジェクト作成→キャンバス→
  タイムライン／コミュニティ画面／有料会員画面／各種設定画面（ジェスチャー・
  パフォーマンス・ペン・バケツ塗り・ワークスペース・データ移行・テーマ・
  ウォーターマーク・フォント・ライセンス・プライバシーポリシー）／ヘルプ・
  ヒント画面／共有・ゴミ箱画面／プロジェクト詳細・素材管理・書き出し・
  自動塗りプリセット・セーブツリー画面）に加え、独立したルートを持たず
  `Navigator.push`（`MaterialPageRoute`）で開く画面3件（自動塗りプリセット
  詳細編集：FAB→新規作成→名前入力→作成→カードタップで到達／ワークスペース
  設定内のPCレイアウト詳細設定：`Icons.dashboard_customize_outlined`ボタン
  タップで到達／コミュニティの投稿者別作品一覧：作品カード→詳細シート→
  投稿者アイコンタップで到達）も対応した。`router.dart`に定義されている
  トップレベルのルートと、主要な`MaterialPageRoute`遷移はほぼ巡回済み。

  実装時の留意点：GoRouterで`push`すると前の画面（ホーム等）が裏に
  残ったまま（マウント済みだが非表示）になるため、両画面に同じ`+`アイコン
  のFABがあるケースなどで`find.byType`が複数ヒットすることがある
  （`push`ではなく`go`でスタックごと置き換えるか、
  `find.descendant(of: find.byType(対象画面クラス), matching: ...)`で
  絞り込むと安定する）。またScaffoldのFABはデフォルトでHeroアニメーション
  の対象になるため、画面遷移直後にタップすると遷移アニメーション中の
  位置でヒットテストに失敗することがあり、既存の300ms×2回のpumpでは
  足りず300ms×3回程度必要になるケースがあった。

  さらに、残っていた3画面も追加でカバーし、合計14シナリオになった。

  - 新規プロジェクト画面のキャンバスサイズプリセット管理画面
    （`Icons.tune`ボタンで到達）。
  - ホーム画面「作品一覧」タブのフォルダ内画面（`ExportEngine.exportsDir`
    へダミーファイルを1件書き込んでフォルダ一覧の表示条件を満たし、
    FAB→フォルダ作成→タップで到達）。
  - 保存データ変更画面（`_SaveModeChangeScreen`、保存スロットを
    削減しようとした際に保存件数が新しい上限を超えている場合のみ表示
    される特殊系）。ツリー方式（無制限）でSaveTreeService.saveAsChildを
    直接6回呼んで保存データを積み上げたうえで、パフォーマンス設定画面の
    品質プリセットを実際にUI操作で「低品質」（スロット5件）へ切り替える
    ことで到達。

  上記2画面（作品一覧タブのフォルダ・保存データ変更画面）は、いずれも
  実際にディスクへ書き込む処理（`getApplicationDocumentsDirectory`）を
  経由するが、`flutter test`環境にはpath_providerの実装が登録されておらず
  未モックだと`MissingPluginException`になる。`mockPathProvider`という
  ヘルパー（`plugins.flutter.io/path_provider`チャンネルを実際の一時
  ディレクトリを返すようモックし、テスト終了時に解除・削除する）を新設し、
  必要なテストでのみ呼び出す方式にした（他のテストへは影響しない）。
  また`ExportEngine.listExportedFiles`は結果を静的にキャッシュしており、
  起動画面のプリフェッチで既に空リストがキャッシュされてしまうため、
  ダミーファイル書き込み後に`forceRefresh: true`で明示的に再取得させる
  必要があった。

  **上記の過程で発見し、別途修正済みの不具合**：ホーム画面「共有」タブ
  （`ProjectService.shared`）が常に空だった件。`.niashare`の共有・受信
  自体（`_createNiashare`／`showMaterialIncludeDialog`によるチェック
  ボックス選択式の軽量共有データ作成、`ShareIntentService`／
  `_handleSharedUri`による受信・インポート確認ダイアログ）は元々
  完全に実装済みだったが、`ProjectService.importSharedProject`が
  追加先を`_projects`（プロジェクト一覧タブ）にしていたため、独立した
  `_shared`（共有タブ専用リスト）には一件も追加されない配線漏れが
  あった。`Project.isSharedImport`フラグを新設し、`shared`ゲッターを
  `_projects`をこのフラグで絞り込むビューへ変更（独立リストは廃止）、
  `importSharedProject`でフラグを立てるよう修正、`moveToSharedFolder`/
  `deleteSharedFolder`も`_projects`側を直接操作するよう修正、ホーム画面
  プロジェクトタブの一覧からは`isSharedImport`のものを除外するよう
  修正した。これにより既存のID検索ベースの編集・書き出し・セーブツリー
  等の処理（`projects.where((p) => p.id == ...)`パターン）はそのまま
  共有インポート分にも機能する。`docs/AI設計書/19_プロジェクト管理仕様.md`
  の該当箇所（.niashareセクション・タップ操作表）も実装済みの実際の
  挙動に合わせて更新済み。`test/project_folder_test.dart`に2件テストを
  追加（共有タブへの反映・専用フォルダ操作）。
- この過程で発見した実際の不具合2件を修正済み：
  - `TimelineScreen`/`CanvasScreen`の`dispose()`内で`context.read<T>()`を
    直接呼んでいた不具合（画面の一括破棄時に例外になり得た）。
  - `ProjectService.duplicateLayer`がTileManagerの実キー（合成キー）では
    なくレイヤーIDをそのまま使っていたため、複製先が空になる不具合。
- Chrome/Edgeでの`flutter run`失敗の原因説明（ローカル環境のファイアウォール/
  VPN等が疑わしいとユーザーへ回答済み。コード側の対応事項ではない）。

## 未着手：ブックマーク機能待ちの項目

- **ホーム画面の並び替え選択肢に「ブクマ順」を追加**：現在は「名前・更新日時」の
  2択のみ。ブックマーク機能（動画投稿・ランキング機能仕様のバックエンド）が
  未実装で、ブックマーク日時等のデータが存在しないため着手不可。バックエンド
  実装後に対応する（ホーム画面の「ブクマ済み」タブ`_BookmarkedTab`も同様に、
  器だけ先行実装済みで中身のつなぎ込みが必要）。

## 未着手：実機でないと真偽が確認できない不具合報告

以下はいずれもコードレビューを複数回実施し、配線・計算式ともに問題は見当たらなかった。
実機のない環境での静的解析には限界があるため、着手する場合はまず実機での再現確認から
始めること。

- **フレーム一覧をタップしても現在地（カレントフレーム）が移動しない**（キャンバスモード側の
  `FrameStripWidget`）。`GestureDetector.onTap`から`canvas_screen.dart`側の
  `setState(() => _currentFrame = idx)`まで配線は正しく繋がっている。中央の
  `IgnorePointer`で囲われた現在フレーム枠がタップを奪っている可能性も確認したが、
  `IgnorePointer`はヒットテスト対象から正しく除外されるため問題なし。再現するなら
  実機のログ（onTapが実際に呼ばれているか）から調査すること。
- **フレーム一覧を左右にスワイプしたとき、中央の現在フレーム枠に一番近いフレームへ
  自動吸着し、そのフレームを現在地にする**（タイムラインモードと同じ挙動に揃える）。
  そのときキャンバス・レイヤーパレット・タイムラインモードの表示も連動すること。
  `_scrollToCurrent`/`_handleScrollEnd`の計算式は数式上つじつまが合っていることを
  手計算で確認済み。
- **プレビュー下端のドラッグハンドルとサイズ連動の見え方**：ドラッグ配線自体は正しいが、
  映像のアスペクト比によっては`BoxFit.contain`のレターボックスが増えるだけで映像本体が
  それ以上大きくならない地点がある（設計上の制約で、単純なコード修正では解決できない）。
  着手する場合は実機で実際の見え方を確認した上で、対応するか仕様として許容するかを
  まず判断すること。

## 未着手：スマホ表示×PC表示の実機未検証事項

PC専用ワークスペースUI（ドッキングパネル・パネル並べ替え・リサイズ・保存ワークスペース等）
一式は実装済みだが、実機のない環境のため、実際の見た目・操作感（レイアウト崩れ・パネルの
重なり・スクロール領域の高さ計算等に問題が無いか）は未検証。特に多数のパネルを同時に
開いた場合の見え方は要確認。

## 未着手：コードでは対応できない事業・法務上の残課題

以下はユーザー自身の実際の事業情報（連絡先・所在地の扱い等）が確定して初めて対応できる
事項であり、コードの修正だけでは解決できない。着手する場合も、ソースコードの変更では
なく、ユーザーへの確認・情報提供の依頼が中心になる。

- **プライバシーポリシーの「お問い合わせ先」が未設定のまま**：全7言語で
  プレースホルダー（「開発者連絡先：未設定」）のままになっている
  （`privacyPolicyArt8Body`）。本アプリ専用の無料Gmailアドレス等をユーザーから
  受け取り次第、全7言語の`privacyPolicyArt8Body`へ反映する。
- **特定商取引法に基づく表示への対応**：Google Playで日本向けに有料アプリ内課金
  （プレミアム機能）を提供する場合、事業者名・電話番号・所在地等を含む表示が別途
  必要になる。住所・電話番号等の実データが確定するまでコード側での対応（専用画面の
  追加等）は行わない（プレースホルダーの偽の事業者情報を実装すると、かえって不正確な
  法的表示を公開することになるため）。

## 進め方の注意

- 実機で確認しないと真偽が分からない項目は、コードレビューだけで「直った」と断定せず、
  正直に「直っている可能性が高いが要確認」と報告すること。
