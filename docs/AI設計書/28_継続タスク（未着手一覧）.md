# 28_継続タスク（未着手一覧）

このファイルは、ユーザーから2026年8月19日に連続して依頼された大量の要望・不具合報告のうち、
まだ解決できていない項目を別セッションへ引き継ぐためのメモ（直近の更新：2026年8月21日、
眼鏡断層フィルターの前提だったレイヤー切り替えバグを調査・修正、選択レイヤーの
オンスクリーン不可視問題を新たに追記）。
実装済みの項目は本ファイルから削除済み（内容は`12_実装チェックリスト.md`の各追記セクションを
参照）。着手前に必ず現状のコードを確認すること（このメモの記述後に状況が変わっている
可能性がある）。

作業時の基本フロー（必ず踏襲する）：
`flutter analyze`（ベースライン70件、0エラー、既存のdeprecated_member_use/use_build_context_synchronouslyのみ）
→ `flutter test`（ベースライン78件全成功）→ `git add -A` → 日本語コミットメッセージ
（末尾に`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`）→
`git fetch origin dev_branch && git log HEAD..origin/dev_branch --oneline`（分岐確認）→
`git push origin dev_branch`。ARBファイルを編集したら必ず`flutter gen-l10n`を実行し直す
（7言語：ja/en/es/fr/ko/zh/zh_Hant、jaがテンプレート）。ARB翻訳は機械的な文字列置換だと
ES/FRなどで訳が崩れることがあるため、ロケールごとに手書きで訳すこと。

## 未着手：実機でないと真偽が確認できない不具合報告

以下はいずれもコードレビューを複数回実施し、配線・計算式ともに問題は見当たらなかった
（詳細は各項目の記述および`12_実装チェックリスト.md`の追記を参照）。実機のない環境での
静的解析には限界があるため、着手する場合はまず実機での再現確認から始めること。

- **フレーム一覧をタップしても現在地（カレントフレーム）が移動しない**（キャンバスモード側の
  `FrameStripWidget`）。各フレームの`GestureDetector.onTap`は`widget.onFrameSelected(index)`を
  呼び、`canvas_screen.dart`側で`onFrameSelected: (idx) => setState(() => _currentFrame = idx)`と
  正しく配線されている。`IgnorePointer`で囲われた中央の現在フレーム枠（2026年8月20日に
  色固定の赤からテーマの更新マーク色連動へ変更済み、`FrameStripWidget`参照）がタップを
  奪っている可能性も確認したが、`IgnorePointer`はヒットテスト対象から正しく除外される
  ため問題なし。再現するなら実機のログ（onTapが実際に呼ばれているか）から調査すること。
- **フレーム一覧を左右にスワイプしたとき、中央の現在フレーム枠に一番近いフレームへ自動吸着し、
  そのフレームを現在地にする**（タイムラインモードと同じ挙動に揃える）。
  そのときキャンバス・レイヤーパレット・タイムラインモードの表示も連動すること。
  `_scrollToCurrent`の`target = currentFrame * itemExtent`と`_handleScrollEnd`の
  `nearest = (offset / itemExtent).round()`は、`sidePadding = (ビューポート幅-itemExtent)/2`
  を用いた中央揃えの計算式として数式上つじつまが合っている（左右パディング分がちょうど
  打ち消し合う）ことを手計算で確認済み。
- **定規ツールをタップしても何も表示されない**という報告があった。`_showRulerPanel`は他の
  正常動作しているパネル（ブラシ・トーン・スタンプ等）と全く同じ`_sidedPanel(...)`パターンで
  画面全体の最前面Stackに配置されており、`_toggleRuler`（トグル）・`_rulerPanel()`（内容）とも
  配線に問題は見当たらない。フローティングパネルを最前面Stackへ移した過去の修正で解消して
  いる可能性が高い。
- **プレビュー下端のドラッグハンドルを動かすと「表示範囲」が変わってしまい「サイズ」が
  連動しない**という報告が一度あった。`onVerticalDragUpdate`が`_previewHeightDragOverride`を
  更新し、`SizedBox(height: previewHeight, child: _buildPreviewContent())`がその値をそのまま
  高さに反映、内側は`Positioned.fill`＋`RawImage(fit: BoxFit.contain)`で外側のサイズに
  アスペクト比を保って収まる作りになっており、理論上は正しくサイズ連動するはず。根本原因は
  特定できていない（ヒット領域拡大のみ対応済み）。
- **チュートリアルの吹き出しがどこをタップしても消えない**という報告があったが、
  `first_use_tooltip.dart`を確認した限り、既に全画面の透明バリアで「どこでも閉じられる」
  実装になっていた。過去に修正した多重表示バグが原因だった可能性がある。

## 未着手：アプリ全体のデザインを「シンプル」から「作り込んだ完成度」へ

2026年8月20日、「プレミアム画面だけでなくアプリ内全体に言えるが、シンプルな
デザインというよりは完成されたデザインという感じでしっかり作り込んでほしい」
という要望があった。ユーザーからは「どの画面から着手しても構わない」との
返答があったため、以下の順で着手済み：

- プレミアム画面（`premium_screen.dart`）：導入バナー・比較表を強化（影・
  グラデーション・3つのハイライト・比較表の縞模様とプレミアム列の差し色敷き）。
- ホーム画面のカードグリッド（`project_list_widget.dart`の`_gridCard`・
  `_folderGridCard`）：選択状態で濃さが変わるアニメーション付きの影、統一した
  角丸、サムネイルへ直接重ねてグラデーションscrimで読みやすくしたプロジェクト名、
  半透明背景付きのお気に入りバッジ・選択チェックマークを追加。
- 設定画面トップ（`settings_screen.dart`の`_item`）：フラットなListTile羅列を、
  影付きMaterial+InkWellのカードとして浮かせる構成へ変更。アイコン背景も
  単色からグラデーションへ。
- ヘルプ画面（`help_screen.dart`）・活用Tips画面（`tips_screen.dart`）：一覧行を
  影付きカード化し、アイコンにグラデーション背景バッジを追加。検索欄も
  角丸・塗りつぶし背景の統一デザインへ変更。
- セーブツリー画面のスロット一覧（`save_tree_screen.dart`の`_SlotTile`）：
  同じ影付きカードへ統一。ツリー表示（接続線描画あり）と選択用サブ画面
  （ダイアログ内のチェックボックスリスト）はレイアウトの性質が異なるため
  対象外とした。

**追加対応済み（2026年8月21日）**：
- テーマ設定画面（`theme_settings_screen.dart`）：カラーカスタマイズ一覧
  （`_ColorCustomizeTile`）とテーマプリセット一覧（`ReorderableListView`
  の各行）を、他画面と同じ影付きMaterial+InkWellのカードへ統一
  （`surfaceContainerLow`背景・角丸14・影`alpha 0.15`）。選択中のプリセット
  行のみ背景をprimaryContainerで区別する扱いは維持。
- ワークスペース設定画面（`workspace_settings_screen.dart`）：ツールバー
  編集・左利きモード・PC/スマホモード切替・既定ドッキングパネル・
  エンドカード設定の各セクションを囲む`Card`5箇所を、素の`Card()`から
  同じ影付き・角丸14・`surfaceContainerLow`背景のスタイルへ統一（内部の
  リスト項目・機能自体は変更なし）。

**それ以外の画面（キャンバス本体・タイムライン・ホーム画面の他の部分＝
ドロワー以外のリスト表示等・その他の設定詳細画面）にはまだ着手して
いない**。範囲が非常に広いため、続きは同じ「影・グラデーション・
統一感のある角丸・バッジの背景」といった作り込みの方向性を踏襲しつつ、
他の画面へ順次広げていくこと。

## 未着手：スマホ表示×PC表示の本格的な差別化（続き）

2026年8月20日、「スマホはすっきりしたUI、PCはブラシ・カラーサークル・
キャンバスプレビュー等が常に表示されているプロ向けの本格的なレイアウト」
という差別化の要望があり、「ごちゃごちゃさせない方針はスマホ版のみで、
PC版は画面が大きいのでいろいろ表示してよい」という明確化を受けた。

**対応済み（2026年8月20日の複数回のやり取りで、以下すべて完了）**：
- `canvas_screen.dart`のPC/DeXドッキング構成を、ツールオプション系
  （ブラシ・トーン・スタンプ・ペンサブツール・オニオンスキン・定規・
  フィルター・早替えツール・色調調整）も含めて全パネルが互いに排他で
  なくなるよう拡張した。開いているものはすべて左側ドッキング領域に
  縦積み表示（`_openToolOptionPanels`・`_dockedPanelStack`、スクロール
  対応）、カラーピッカー・レイヤーパネル・キャンバスプレビューは右側
  ドッキング領域に縦に並べて表示する。`_closeAllOverlayPanels()`も
  PC/DeXモードでは対象パネル全体（自由変形/メッシュ変形パネルを除く）
  を除外するようにした。スマホ版は「一度に1枚のみ」の既存方針をそのまま
  維持（フローティング表示のため重なると閉じるボタンが押せなくなる
  不具合があった経緯があるため）。
- 「キャンバスプレビュー」＝ナビゲーター（拡大表示中も全体を縮小表示で
  確認できるパネル）として`CanvasPreviewNavigator`を新規実装。長辺200px
  まで縮小した軽量合成を0.9秒間隔で更新。キャンバス上部バーの設定/編集
  メニューから開閉、PC/DeXモード専用。
- 「どのパネルを既定で開いておくか」をワークスペースプリセットへ保存・
  復元する仕組みを実装した。`SettingsService.defaultDockedPanels`
  （`CanvasDockPanel`のSet、永続化）を新設し、キャンバス初回表示時の
  自動ドッキングはこの設定に従う。設定＞ワークスペースに「PC版で既定で
  開くパネル」チェックリスト（12種、即時反映）を追加。
  `WorkspacePreset`・`WorkspacePresetService`（save/overwrite/rename/
  importPresetFile）にも`defaultDockedPanels`フィールドを追加し、
  名前を付けて保存・上書き保存・読み込み・共有（.niaworkspace）の
  すべてで一緒に保存・復元される。

**未着手**：
- 実機のない環境のため、今回の一連の変更が実際の見た目・操作感
  （レイアウト崩れ・パネルの重なり・スクロール領域の高さ計算等に問題が
  無いか）は未検証。特に多数のパネルを同時に開いた場合の見え方は要確認。

## 未着手：コードでは対応できない事業・法務上の残課題

以下はユーザー自身の実際の事業情報（連絡先・所在地の扱い等）が確定して初めて対応できる
事項であり、コードの修正だけでは解決できない。着手する場合も、ソースコードの変更では
なく、ユーザーへの確認・情報提供の依頼が中心になる。

- **プライバシーポリシー・利用規約の「お問い合わせ先」が未設定のまま**：全7言語で
  プレースホルダー（「開発者連絡先：未設定」）のままになっている。ChatGPTの助言どおり、
  普段使いの個人アドレスとは別に、本アプリ専用の無料Gmailアドレス（例：
  `niarim.app@gmail.com`のような形）を新規作成し、そのアドレスをユーザーから
  受け取り次第、全7言語の該当箇所（`privacyPolicyArt8Body`・`licenseTermsArt8Body`
  周辺）へ反映する。
- **特定商取引法に基づく表示への対応**：Google Playで日本向けに有料アプリ内課金
  （プレミアム機能）を提供する場合、事業者名・電話番号・所在地等を含む表示が別途
  必要になる。ユーザーは自宅住所の公開を避けたい意向があるため、バーチャルオフィスの
  利用要否を含めてユーザー自身が判断する事業上の意思決定であり、住所・電話番号等の
  実データが確定するまでコード側での対応（専用画面の追加等）は行わない
  （プレースホルダーの偽の事業者情報を実装すると、かえって不正確な法的表示を
  公開することになるため）。

## 完了：眼鏡断層フィルター（度入りレンズの光学的な歪み再現、2026年8月21日実装）

度の強い眼鏡レンズの奥が小さく／大きく歪んで見える現象（度数入りレンズの
光学屈折による輪郭のずれ）を再現するフィルターを、選択レイヤー機能の
新規実装とあわせて一通り実装した。

- **選択レイヤー機能**（`lib/models/layer.dart`の`LayerType.selection`）：
  従来「内部専用・パネル非表示」だったコメント・実装を、ユーザーの意向
  どおり「常設・既存の描画ツールで塗れる」方針へ変更した。
  `layer_panel.dart`の`_visibleLayers`から除外条件を削除してパネルに常設
  表示し、レイヤー追加メニューへ「選択レイヤー」を追加（アイコンは
  `Icons.highlight_alt`、色はユーザーの指示どおりテーマの選択色
  `Theme.of(context).colorScheme.secondary`＝`ThemeService`の
  `selectionColor`を使用）。`layer_compositor.dart`の`pixelLayerTypes`
  からは引き続き除外し、最終成果物（キャンバス表示・書き出し）には
  写り込まない。
- **選択レイヤーへの描画中の可視化**：`pixelLayerTypes`除外により通常の
  合成では見えないという問題（後述の別項で判明していた）に対応するため、
  `canvas_area.dart`へ専用のオーバーレイ描画パスを追加した。選択レイヤー
  が存在する場合、その内容を単体合成した`_selectionLayerOverlayImage`を
  常に保持・更新し（`_refreshSelectionLayerOverlay()`、レイヤー切替・
  ストローク確定のたびに再合成）、`_CanvasPainter`側で前面レイヤー群の
  直後・確定選択範囲の直前に、`BlendMode.srcIn`でテーマの選択色（50%
  透明度）にタイントして重ねて描画する。最終合成（`_belowImage`・
  `_compositeImage`・`_aboveImage`）には一切含まれない。
- **フィルター本体**（`FilterKind.lensDistortion`、`FilterEngine.
  applyLensDistortion`）：選択レイヤーのマスクを4連結のフラッドフィルで
  連結成分ごとに分割し（両目分など複数レンズを同時選択して一括適用
  可能）、各成分の重心（＋`lensCenterOffsetX`／`Y`によるpx単位の手動
  オフセット、全成分共通）を中心に、`applyFisheye`と同じ
  `r^exponent`型の局所放射ワープを適用する。`strength`は-100〜100の
  符号付き（0=無効、負=凹レンズ風に縮小、正=凸レンズ風に拡大）。
  マスク境界の半透明画素はワープ後と元の色をアルファブレンドし継ぎ目を
  目立たなくする。選択レイヤーが無い・空の場合は何もしない。
- **UI配線**：`filter_panel.dart`のプレビュー（`_loadPreviewBase`で選択
  レイヤーも同解像度に縮小して保持）・本適用（`_applyToFrame`でフル
  解像度のマスクを取得し`compute()`経由のisolateへ渡す）両方に対応。
  強さ・中心オフセットX・Yの3本のスライダーを追加し、選択レイヤーが
  無い場合はエラー色の案内文を表示する。ヘルプ・Tips（活用Tips＝
  `tips_screen.dart`、演出フィルターカテゴリ）にも使い方を追記した。
- **単体テスト**：`test/filter_engine_lens_distortion_test.dart`を新規
  作成し、マスク無効時・strength=0時の無変化、マスク範囲外の画素が
  完全不変であること、範囲内で実際に歪みが起きること、複数連結成分の
  同時処理が例外なく動作することを検証（実機がない環境のため、視覚的な
  最終確認の代わりに自動テストで最低限の正しさを担保）。

flutter analyze（70 issues、0 errors、既存件数から変化なし）・
flutter test（83 tests passed＝既存78件＋新規5件）で検証済み。実機が
ないため、実際の見た目（キャンバス上のオーバーレイ表示・フィルター適用
結果の視覚的な妥当性）は未検証。着手する場合はまず選択レイヤーを追加して
円や楕円を塗り、キャンバス上に選択色のオーバーレイが表示されること、
フィルターパネルでプレビューが更新されること、実際に適用した結果が
狙った歪みになっているかを確認すること。

### 完了：レイヤー切り替えと実際の描画対象の紐付け経路（2026年8月21日、調査・修正済み）

前回の調査結果（`layer_panel.dart`の`_selectedIndex`と`canvas_screen.dart`
の`_currentLayerId`が別々の変数で、両者を同期させるコールバックが
見つからない）を再検証した結果、**「静的解析で追いきれていない別経路が
あるはず」という推測は誤りで、実際に同期の仕組みが存在しない、確認済みの
バグだった**。`_currentLayerId`は`canvas_screen.dart`全体でも3箇所
（宣言・初期化・`CanvasArea`への受け渡し）にしか出現せず、`LayerPanel`
側の行タップ（`onTap`）はローカルの`_selectedIndex`を更新するだけで、
親（`canvas_screen.dart`）へ選択変更を伝えるコールバックは存在しなかった。
つまり**レイヤーパネルで別の行をタップしてハイライト表示は変わっても、
実際にペンストロークが書き込まれる先（`_currentLayerId`）は最初の
レイヤーのまま変わらない**という、地味だが実描画に直結する不具合だった
（`layer_panel.dart`の`_lastUndoCount`ブロックが`layers[_selectedIndex]`
のサムネイルだけを更新している箇所からも、「`_selectedIndex`＝実際の
描画対象」という前提でコードが書かれていたことが読み取れ、単に配線が
漏れていたと判断した）。

**修正内容**：`LayerPanel`に`currentLayerId`（外部から見た実際の描画対象、
パネル初回表示時のハイライト同期用）と`onLayerSelected`（行タップ時に
実際の描画対象IDを親へ伝えるコールバック。フォルダ行は描画対象になり
得ないため対象外）を追加し、`canvas_screen.dart`側の2箇所の`LayerPanel`
呼び出し（スマホ版フローティング表示・PC/DeX版ドッキング表示）双方で
`currentLayerId: _currentLayerId`・
`onLayerSelected: (id) => setState(() => _currentLayerId = id)`を配線した。
既存の`_selectedIndex`ベースのロジック（ハイライト・サムネイル更新・
削除/結合ボタンの活性制御等）は一切変更しておらず、新規のコールバック配線
のみの追加のため、既存機能への影響は最小限のはず（実機未検証。着手する
場合はまずレイヤーを2枚以上作り、パネルで切り替えてから描画し、狙った
レイヤーに正しく描かれるかを確認すること）。

## 進め方の注意

- 実機で確認しないと真偽が分からない項目は、コードレビューだけで「直った」と断定せず、
  正直に「直っている可能性が高いが要確認」と報告すること。
