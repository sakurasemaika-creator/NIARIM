# 28_継続タスク（未着手一覧）

このファイルは、ユーザーから2026年8月19日に連続して依頼された大量の要望・不具合報告のうち、
まだ解決できていない項目を別セッションへ引き継ぐためのメモ。実装済みの項目は本ファイルから
削除済み（内容は`12_実装チェックリスト.md`の各追記セクションを参照）。着手前に必ず現状の
コードを確認すること（このメモの記述後に状況が変わっている可能性がある）。

作業時の基本フロー（必ず踏襲する）：
`flutter analyze`（ベースライン67件、0エラー、既存のdeprecated_member_use/use_build_context_synchronouslyのみ）
→ `flutter test`（ベースライン78件全成功）→ `git add -A` → 日本語コミットメッセージ
（末尾に`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`）→
`git fetch origin dev_branch && git log HEAD..origin/dev_branch --oneline`（分岐確認）→
`git push origin dev_branch`。ARBファイルを編集したら必ず`flutter gen-l10n`を実行し直す
（7言語：ja/en/es/fr/ko/zh/zh_Hant、jaがテンプレート）。

## 未着手：色調調整機能＋ユーザー作成フィルターの管理（大きめ、要アーキテクチャ変更）

2026年8月20日の依頼。以下は関連する一連の機能で、まとめて設計・実装すること。

- **色調調整ツール**：キャンバスモード画面右上の設定・編集メニュー内に、彩度・明度・
  コントラストを調整できる「色調調整」を追加する。ライブプレビューで確認しながら
  調整し、以下のボタンを用意する：
  - 「適用」（そのまま現在のレイヤーへ適用）・「キャンセル」
  - 「描画フィルターに追加する」・「演出フィルターに追加する」
    （調整した内容を、新しいカスタムフィルターとしてフィルター一覧に保存する）
  - エンジン側：`FilterEngine`に彩度・明度・コントラストの3パラメータを持つ
    新しい変換メソッドが必要（`FilterKind.monochrome`と同様の単純な1画素ずつの
    処理で足りるはず）。
- **⚠️アーキテクチャ上の注意**：現在の描画フィルター（`FilterKind`・`FilterService`）は
  「種別ごとに1つずつの固定セット」という設計になっている
  （`filter_panel.dart`のコメント参照：「仕様書18の初期実装フィルターは種別ごとに1つずつの
  固定セットで、ユーザーが新規フィルターを追加できるUIは無いため、kindから一意に決まる」）。
  「色調調整からフィルターに追加する」を実現するには、ユーザーが**同じ種別のフィルターを
  複数・好きな名前で追加できる**ようにする設計変更が必要（`FilterDef`はIDベースなので
  複数保持自体は可能だが、`filter_panel.dart`の表示名・アイコン取得ロジックが
  `kind`から一意に決まる前提になっている点を直す必要がある）。演出フィルター
  （`EffectFilterInstance`）は既に複数インスタンスに対応済みなのでそちらは追加が楽。
- **フィルター一覧の三点メニュー（新規）**：プリインストールではない、ユーザーが新規追加
  したフィルターにのみ、フィルターの右上に三点メニューを付け「削除」「複製」を
  できるようにする（プリインストール分にはメニューを出さない、または「複製」のみ許可する
  等の切り分けが必要）。
- **お気に入り登録中は削除不可**：フィルターは既にお気に入り登録・解除ができる
  （`FilterDef.isFavorite`、`FilterService`）。お気に入り登録したまま削除しようとしたら、
  「お気に入り登録を解除してから削除してください」という主旨のポップアップを出して
  削除をブロックする。

## 未着手：ヘルプページの項目並び替え（軽微）

- 統合しきれない項目も、関連する項目同士がヘルプ一覧内で近くに並ぶよう順番を入れ替える。
  → 既存の大分類ごとのセクション分けである程度は達成されているが、本格的な並び替えは
  まだ未実施。

## 未着手：実機でないと真偽が確認できない不具合報告

以下はいずれもコードレビューを複数回実施し、配線・計算式ともに問題は見当たらなかった
（詳細は各項目の記述および`12_実装チェックリスト.md`の追記を参照）。実機のない環境での
静的解析には限界があるため、着手する場合はまず実機での再現確認から始めること。

- **フレーム一覧をタップしても現在地（カレントフレーム）が移動しない**（キャンバスモード側の
  `FrameStripWidget`）。各フレームの`GestureDetector.onTap`は`widget.onFrameSelected(index)`を
  呼び、`canvas_screen.dart`側で`onFrameSelected: (idx) => setState(() => _currentFrame = idx)`と
  正しく配線されている。`IgnorePointer`で囲われた中央の赤枠がタップを奪っている可能性も
  確認したが、`IgnorePointer`はヒットテスト対象から正しく除外されるため問題なし。
  再現するなら実機のログ（onTapが実際に呼ばれているか）から調査すること。
- **フレーム一覧を左右にスワイプしたとき、中央の赤枠に一番近いフレームへ自動吸着し、
  そのフレームを現在地にする**（タイムラインモードと同じ挙動に揃える）。
  そのときキャンバス・レイヤーパレット・タイムラインモードの表示も連動すること。
  `_scrollToCurrent`の`target = currentFrame * itemExtent`と`_handleScrollEnd`の
  `nearest = (offset / itemExtent).round()`は、`sidePadding = (ビューポート幅-itemExtent)/2`
  を用いた中央揃えの計算式として数式上つじつまが合っている（左右パディング分がちょうど
  打ち消し合う）ことを手計算で確認済み。
- **定規ツールをタップしても何も表示されない**という報告があった。`_showRulerPanel`は他の
  正常動作しているパネル（ブラシ・トーン・スタンプ等）と全く同じ`_sidedPanel(...)`パターンで
  画面全体の最前面Stackに配置されており、`_toggleRuler`（トグル）・`_rulerPanel()`（内容）とも
  配線に問題は見当たらない。フローティングパネルを最前面Stackへ移した過去の修正で解消して
  いる可能性が高い。
- **プレビュー下端のドラッグハンドルを動かすと「表示範囲」が変わってしまい「サイズ」が
  連動しない**という報告が一度あった。`onVerticalDragUpdate`が`_previewHeightDragOverride`を
  更新し、`SizedBox(height: previewHeight, child: _buildPreviewContent())`がその値をそのまま
  高さに反映、内側は`Positioned.fill`＋`RawImage(fit: BoxFit.contain)`で外側のサイズに
  アスペクト比を保って収まる作りになっており、理論上は正しくサイズ連動するはず。根本原因は
  特定できていない（ヒット領域拡大のみ対応済み）。
- **チュートリアルの吹き出しがどこをタップしても消えない**という報告があったが、
  `first_use_tooltip.dart`を確認した限り、既に全画面の透明バリアで「どこでも閉じられる」
  実装になっていた。過去に修正した多重表示バグが原因だった可能性がある。

## 進め方の注意

- 実機で確認しないと真偽が分からない項目は、コードレビューだけで「直った」と断定せず、
  正直に「直っている可能性が高いが要確認」と報告すること。
