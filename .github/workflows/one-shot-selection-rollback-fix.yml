name: One-shot selection rollback fix

on:
  push:
    branches:
      - dev_branch
    paths:
      - '.github/workflows/one-shot-selection-rollback-fix.yml'
      - 'scripts/one_shot_selection_rollback_fix.py'

permissions:
  contents: write

jobs:
  verify-and-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout dev_branch
        uses: actions/checkout@v6
        with:
          ref: dev_branch
          fetch-depth: 0

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: false

      - name: Apply guarded patch
        run: python3 scripts/one_shot_selection_rollback_fix.py

      - name: Format changed files
        run: dart format lib/engine/tile_manager.dart lib/screens/canvas/widgets/canvas_area.dart test/functional_audit_batch19_test.dart

      - name: Get dependencies
        run: flutter pub get

      - name: Functional selection rollback tests
        run: |
          flutter test test/functional_audit_batch19_test.dart --reporter expanded
          flutter test test/functional_audit_batch18_test.dart --reporter expanded

      - name: Analyze changed scope
        run: flutter analyze --no-fatal-infos --no-fatal-warnings lib/engine/tile_manager.dart lib/screens/canvas/widgets/canvas_area.dart test/functional_audit_batch19_test.dart

      - name: Commit verified patch
        run: |
          git config user.name "NIARIM Audit Bot"
          git config user.email "actions@users.noreply.github.com"
          git add lib/engine/tile_manager.dart lib/screens/canvas/widgets/canvas_area.dart test/functional_audit_batch19_test.dart
          git diff --cached --quiet && exit 0
          git commit -m "fix: rollback interrupted selection transforms"
          git push origin HEAD:dev_branch
