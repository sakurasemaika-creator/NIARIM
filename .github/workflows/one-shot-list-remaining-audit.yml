name: One-shot Apply Existing Visual Evidence

on:
  push:
    branches: [dev_branch]
    paths:
      - '.github/workflows/one-shot-list-remaining-audit.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev_branch
          fetch-depth: 0
      - name: 既存Artifactで確認済み2項目をfinal_passへ反映
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path
          path=Path('audit-dashboard/feature-audit-manifest.json')
          data=json.loads(path.read_text())
          confirmed={'ui.canvas_panels','filter.all'}
          found=set()
          for f in data['features']:
              if f.get('id') in confirmed:
                  found.add(f['id'])
                  for k in ['interaction','output','screenshot','visual','final_pass']:
                      f[k]=True
                  if f['id']=='ui.canvas_panels':
                      f['evidence_note']='Audit Continuous Queue #24 canvas panel screenshot audit passed; 10 panel screenshots visually reviewed.'
                  else:
                      f['evidence_note']='Audit Continuous Queue #24 visual_reaudit_filters passed; all 21 filter output images visually reviewed.'
          missing=confirmed-found
          if missing: raise SystemExit(f'missing ids: {sorted(missing)}')
          path.write_text(json.dumps(data,ensure_ascii=False,indent=2)+'\n')
          print('confirmed',sorted(found))
          print('final_pass',sum(bool(f.get('final_pass')) for f in data['features']))
          PY
      - name: dev_branchへ反映
        run: |
          git config user.name "NIARIM Audit Bot"
          git config user.email "actions@users.noreply.github.com"
          git add audit-dashboard/feature-audit-manifest.json
          git commit -m "既存視覚証拠で確認済み2項目を最終確認済みに更新"
          git pull --rebase origin dev_branch
          git push origin HEAD:dev_branch
