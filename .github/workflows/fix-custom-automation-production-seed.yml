name: Repair Custom Automation Visual Canvas Seed

on:
  push:
    branches:
      - dev_branch
    paths:
      - '.github/workflows/fix-custom-automation-production-seed.yml'
      - 'test/custom_automation_production_surface_visual_test.dart'
      - 'tool/patch_custom_automation_surface_interactions.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev_branch
          fetch-depth: 0
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - run: python3 tool/patch_custom_automation_surface_interactions.py
      - run: dart format test/custom_automation_production_surface_visual_test.dart
      - run: flutter analyze test/custom_automation_production_surface_visual_test.dart --no-fatal-warnings --no-fatal-infos
      - run: flutter test test/custom_automation_production_surface_visual_test.dart --reporter expanded
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: custom-automation-production-seed-evidence
          path: build/visual-reaudit/custom-automation-production
          if-no-files-found: warn
          retention-days: 7
      - if: success()
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/workflows/fix-custom-automation-production-seed.yml
          git rm tool/patch_custom_automation_visual_seed.py
          git rm tool/patch_custom_automation_visual_route_lifecycle.py
          git rm tool/patch_custom_automation_surface_interactions.py
          git rm test/custom_automation_production_visual_test.dart
          git mv test/custom_automation_production_surface_visual_test.dart test/custom_automation_production_visual_test.dart
          git add test/custom_automation_production_visual_test.dart
          git commit -m 'test: stabilize production automation visual flow'
          git pull --rebase origin dev_branch
          git push origin HEAD:dev_branch
